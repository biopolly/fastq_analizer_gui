
# 🧬 FASTQ Analyzer

Простой и мощный анализатор FASTQ файлов с графическим интерфейсом! ✨

## 📖 О программе

**FASTQ Analyzer** - это удобное desktop-приложение для анализа файлов в формате FASTQ, которое предоставляет comprehensive статистику о ваших sequencing данных. Программа идеально подходит для биоинформатиков, молекулярных биологов и исследователей, работающих с NGS данными.

### 🎯 Ключевые возможности:

- **📊 Полная статистика** по последовательностям
- **🔍 Анализ качества** чтений
- **🧮 Подсчет состава** нуклеотидов
- **🎨 Интуитивный интерфейс** с цветовым кодированием
- **⚡ Высокая производительность** обработки

## 🏗️ Архитектура программы

### 🔧 Класс FASTQProcessor

**Основной движок анализа** FASTQ файлов:

```python
class FASTQProcessor:
    """Центральный класс для парсинга и анализа FASTQ файлов"""
```

#### 📌 Основные методы:

- **`parse_fastq(file_path)`** - главный метод парсинга файла
  - Автоматически определяет сжатие (.gz)
  - Обрабатывает блоки по 4 строки (стандарт FASTQ)
  - Собирает comprehensive статистику

- **`_count_bases(sequence)`** - подсчет нуклеотидов A,T,G,C
  - Конвертирует в верхний регистр
  - Игнорирует нестандартные символы

- **`_analyze_quality(quality_string)`** - анализ качества Phred33
  - Конвертирует ASCII символы в числовые значения
  - Вычисляет статистики качества

- **`get_basic_stats()`** - возвращает базовую статистику:
  - Количество последовательностей
  - Длины (средняя, минимальная, максимальная)
  - Общая длина всех ридов

- **`get_quality_stats()`** - статистика качества:
  - Среднее качество по всем позициям
  - Минимальное и максимальное качество
  - Общее количество оценок

- **`get_base_composition()`** - состав оснований:
  - Абсолютные количества A,T,G,C
  - Процентное соотношение
  - GC-контент (косвенно)

### 🖥️ Класс FASTQAnalyzer

**Графический интерфейс пользователя**:

```python
class FASTQAnalyzer:
    """GUI класс для визуализации анализа FASTQ файлов"""
```

#### 📌 Компоненты интерфейса:

- **Главное окно** с responsive дизайном
- **Панель управления** с кнопками:
  - 🟡 "Выбрать FASTQ файл" - открытие файлового диалога
  - 🎀 "Проанализировать" - запуск обработки

- **Информационная панель** - отображение выбранного файла
- **Три текстовые области** для разных типов статистики

#### 📌 Методы GUI:

- **`create_interface()`** - построение всего интерфейса
- **`create_text_areas()`** - создание областей вывода
- **`select_file()`** - обработка выбора файла
- **`analyze_file()`** - координация процесса анализа
- **`display_results()`** - отображение результатов

## 🚀 Как использовать

### 📥 Установка и запуск

1. **Убедитесь, что установлен Python 3.6+** ✅
2. **Скачайте файл `fastq_analyzer.py`** 📄
3. **Запустите программу**:
   ```bash
   python fastq_analyzer.py
   ```

### 🔄 Процесс работы

1. **Загрузка данных** 📂
   - Нажмите "Выбрать FASTQ файл"
   - Выберите файл (.fastq, .fq, .gz)
   - Поддерживаются сжатые и несжатые форматы

2. **Анализ** 🔍
   - Нажмите "Проанализировать"
   - Программа обработает файл
   - Появится индикатор прогресса

3. **Просмотр результатов** 📊
   - **Базовая статистика**: обзор основных параметров
   - **Качество**: метрики качества последовательностей  
   - **Состав оснований**: распределение A,T,G,C



## 🛠️ Технические детали

### 📋 Поддерживаемые форматы:
- ✅ Standard FASTQ (.fastq, .fq)
- ✅ Gzip compressed (.fastq.gz, .fq.gz)
- ✅ Файлы любой размерности

### 🔧 Требования:
- **Python**: 3.6 или выше
- **Библиотеки**: только стандартные
- **Память**: зависит от размера файла






